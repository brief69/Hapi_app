


import 'package:flutter/material.dart';

class ProfileTab extends StatelessWidget {
  const ProfileTab({super.key});

  @override
  Widget build(BuildContext context) {
    // ここにProfileタブの内容を実装します。
    return const Center(child: Text('Profile'));
  }
}// プレース